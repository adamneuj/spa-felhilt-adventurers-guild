import { render } from '@testing-library/react'
import Header from './Header'

test('Header', () => {
    describe('<Header>', () => {
        it('works?', () => {
            render(<Header></Header>)
        })
    })
})
